<view class="container">
  <!-- 头部横幅 -->
  <view class="banner">
    <view class="banner-content">
      <text class="banner-title">🏂 雪板价格监控</text>
      <text class="banner-desc">实时追踪最新雪板价格和折扣</text>
    </view>
  </view>

  <!-- 数据统计 -->
  <view class="stats-grid">
    <view class="stat-card">
      <text class="stat-number">{{stats.total}}</text>
      <text class="stat-label">总产品数</text>
    </view>
    <view class="stat-card">
      <text class="stat-number">{{stats.brands}}</text>
      <text class="stat-label">品牌数量</text>
    </view>
    <view class="stat-card">
      <text class="stat-number">{{stats.lastUpdated.split(' ')[0]}}</text>
      <text class="stat-label">最后更新</text>
    </view>
  </view>

  <!-- 最新雪板 -->
  <view class="section">
    <view class="section-header">
      <text class="section-title">最新雪板</text>
      <text class="section-more" bindtap="goToSnowboards">查看更多</text>
    </view>
    
    <view class="product-list">
      <view 
        wx:for="{{latestProducts}}" 
        wx:key="id" 
        class="product-item" 
        bindtap="viewProduct" 
        data-product="{{item}}"
      >
        <image src="{{item.image}}" mode="aspectFill" class="product-image"></image>
        <view class="product-info">
          <text class="product-brand">{{item.brand}}</text>
          <text class="product-name">{{item.name}}</text>
          <text class="product-price">{{item.current_price || '价格待定'}}</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 加载状态 -->
  <view wx:if="{{loading}}" class="loading">
    <text>加载中...</text>
  </view>
</view>